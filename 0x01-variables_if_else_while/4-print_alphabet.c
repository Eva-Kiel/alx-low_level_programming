#include <stdio.h.

/**
 * main - Remove some alphabets
 *
 *Return: Always succesful
 */
int main(void)
{
	char ch;

	for (ch = a'; ch <= 'z'; c++)
	{
		if (c != 'e'&& c != 'q')
		{
			putchar(ch);
		}
	}
	putchar('\n');
	return (0);
}
